return {

  {
    "catppuccin/nvim",
    lazy = true,
    name = "catppuccin",
    opts = {
      -- Disabled by default.
      -- Must be enabled; otherwise, lazygit and vtrem use an ugly colorscheme.
      term_colors = true,
    },
  },
}
